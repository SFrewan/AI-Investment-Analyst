{
  "//": "Runtime configuration. The dashboard is served by the API by default, so an empty base address means 'the origin this page came from'. Point it elsewhere to run the dashboard against another host. No secret belongs in this file - the operator key is entered at sign-in and never stored here.",
  "Api": {
    "BaseAddress": ""
  }
}
